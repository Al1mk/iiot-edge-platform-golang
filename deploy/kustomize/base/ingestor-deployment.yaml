apiVersion: apps/v1
kind: Deployment
metadata:
  name: ingestor
  namespace: iiot
  labels:
    app.kubernetes.io/name: ingestor
    app.kubernetes.io/part-of: iiot-edge-platform
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: ingestor
  template:
    metadata:
      labels:
        app.kubernetes.io/name: ingestor
        app.kubernetes.io/part-of: iiot-edge-platform
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9091"
        prometheus.io/path: "/metrics"
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532   # distroless nonroot uid
        runAsGroup: 65532
      containers:
        - name: ingestor
          image: iiot-ingestor:dev
          imagePullPolicy: IfNotPresent
          env:
            - name: MAX_TS_SKEW
              value: "24h"
            - name: INGESTOR_DB_PATH
              value: /tmp/iiot.db
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
            - name: metrics
              containerPort: 9091
              protocol: TCP
          # Readiness: probe /readyz — pod only gets traffic once the HTTP server
          # is up AND the SQLite store is reachable. This is a true readiness
          # gate; it differs from /healthz which is a shallow liveness check.
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          # Liveness: also probe /healthz — restart if the server stops responding.
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 20
            failureThreshold: 3
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "100m"
              memory: "128Mi"
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}

