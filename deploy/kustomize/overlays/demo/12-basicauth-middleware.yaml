# Traefik BasicAuth middleware — Demo only
# Contact: alimk1752@gmail.com
# API group: traefik.io/v1alpha1 (k3s >= v1.29 / Traefik v2.10+)
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: iiot-basicauth
  namespace: iiot
spec:
  basicAuth:
    secret: iiot-basic-auth
    realm: "IIOT Demo"
